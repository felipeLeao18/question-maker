/**
 * @swagger
 * definitions:
 *     Course:
 *       properties:
 *         _id:
 *           type: string
 *         name:
 *           type: string
 *         description:
 *           type: string
 *         users:
 *           type: array
 *           items:
 *             type: string
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 *       required:
 *         - name
 *       type: object
 */
export interface ICourse {
  name: string
  description: string
  users: string[]
}
